import 'package:flutter/material.dart';
class HelpStack extends StatelessWidget {
  const HelpStack({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        color: Colors.black.withAlpha(50),
      ),
      child: Stack(
        children: <Widget>[

        ]
      ),
    );
  }
}
